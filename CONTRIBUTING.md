# Contributing to BioX

- Use Python 3.9+
- Run `pre-commit install`
- Lint and format: `pre-commit run -a`
- Run tests: `pytest`
- Type check: `mypy src`

## Pull Requests

- Include tests for new features and bugfixes.
- Update documentation and `CHANGELOG.md`.
